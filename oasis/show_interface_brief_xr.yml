---
- name: PLAYBOOK SHOW VERSION
  hosts: pe_router
  gather_facts: no
  connection: network_cli

  tasks:
    - name: run show version on remote devices
      ansible.netcommon.cli_command:
        command: '{{item}}'
      loop:
        - show interface
        - show ip route